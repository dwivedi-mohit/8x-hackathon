# Server only. Never expose this value in browser code or commit a real key.
OPENAI_API_KEY=

# Pin the supported Realtime model selected for the demo.
OPENAI_REALTIME_MODEL=gpt-realtime
PORT=3001

# Comma-separated browser origins allowed to call this server in development.
ALLOWED_ORIGINS=http://localhost:5173
